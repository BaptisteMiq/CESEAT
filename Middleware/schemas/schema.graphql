# import User, UserInput from "users.graphql"

type Query {
    users: [User!]
    test: String
}

type Mutation {
    createUser(user: UserInput): User
}

schema {
    query: Query
    mutation: Mutation
}
